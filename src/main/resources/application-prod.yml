server:
  address: 0.0.0.0
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://jby-surl-project-db.internal:5432/surl_prod
    username: postgres
    password: y3Fm1ezNaxMlLPQ
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false
        highlight_sql: false
        use_sql_comments: false
logging:
  level:
    com.koreait.surl_project_11: INFO
    org.hibernate.SQL: INFO
    org.hibernate.orm.jdbc.bind: INFO
    org.hibernate.orm.jdbc.extract: INFO
    org.springframework.transaction.interceptor: INFO
custom:
  site:
    name: "My Site Prod"